export interface IPerson {
    name: string;
    title: string;
    bio: string;
    picture: string;
    github?: string;
    portfolio?: string;
    linkedin?: string;
    email?: string;
    kaggle?: string;
}
